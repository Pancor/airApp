<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.AppTheme" />

    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="SplashTheme" parent="Base.AppTheme">
        <item name="android:windowBackground">@drawable/activity_splash</item>
    </style>

    <!-- CardView used in fragment_nearest_station.xml -->
    <style name="CustomCardView" parent="CardView">
        <item name="contentPaddingTop">16dp</item>
        <item name="contentPaddingBottom">16dp</item>
        <item name="contentPaddingLeft">16dp</item>
        <item name="contentPaddingRight">16dp</item>
        <item name="cardCornerRadius">4dp</item>
        <item name="cardBackgroundColor">@android:color/white</item>
        <item name="cardElevation">4dp</item>
    </style>

</resources>
